{% extends 'base.html.twig' %}

{# On surcharge le bloc parent  #}
{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}
<h1 class="text-center mt-5">{{ title }}</h1>
    <p class="mt-5 text-center">{{ homepage_text }}</p>
{% endblock %}
