{% extends 'base.html.twig' %}

{# On surcharge le bloc parent  #}
{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}
<h1>{{ title }}</h1>
    <p>{{ homepage_text }}</p>
{% endblock %}
